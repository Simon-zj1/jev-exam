# 更新日志

## v0.7.0 · 2026-09-26

### 新增

- **数据导出（三种格式，都能脱离本站使用）**：Markdown（材料正文 + 题目与评分点 + 学习状态）、
  Anki CSV（复习卡片，RFC 4180 转义）、完整备份 JSON（材料、题目、作答、判定、掌握度、
  复习排期、纠错记录、用量）。入口在设置页，走 `GET /api/export/{markdown,anki,backup}`，
  文件名同时给 ASCII 兜底与 `filename*`，中文名不会变乱码。
- **用户纠错回流**：结果页每题新增「这题判错了？」，提交时会**冻结当时的现场**
  （题目、作答原文、逐得分点概率、引擎与模型版本），存进 `feedback_reports`。
  `npm run feedback:golden` 可导出成金标准候选，人工确认后并入评测集——
  这是把标注集做大的天然来源。
- **上传体检**：解析后给出可读结论（文字覆盖率、正文体量、段落还原、乱码检测、页码追溯），
  分「可用 / 基本可用 / 质量不佳」，扫描件不再静默变成一份残缺材料。结论存在
  `source_map.health` 并在材料页展示。
- **模型成本计量**：新增 `llm_usage` 表，按天 + 模型记录调用次数、输入/输出 token 与
  估算费用（微美元整数，价格表见 `src/lib/llm/usage.ts`）。出题、判定基线与材料问答
  统一经 `RecordingChatProvider` 计量，设置页可看今日成本。
- **账号删除**：设置页输入邮箱二次确认后删除账号与全部派生数据（App Store 的上架前置条件）。
- **交错练习**：复习队列在到期时间之外，再按知识点打散出示顺序，避免同一知识点的卡片连着出现。
- **服务条款与隐私说明**：新增 `/terms` 与 `/privacy` 两个页面，写清数据处理、版权责任、
  生成内容免责与下架渠道；落地页新增「为什么不用 ChatGPT / NotebookLM」的四点对比。
- **公开基准**：[docs/benchmark.md](docs/benchmark.md) 记录数据格式、复现命令与指标门槛，
  `npm run benchmark` 会写出机器可读的 `docs/benchmark-results.json`。

### 变更

- 额度新增问答维度后，额度卡与设置页同步展示；`ask` 默认 200 次/天。

### 修复

- SECURITY.md 仍写着「暂不支持 PDF/扫描件解析」，v0.6.0 起已支持，属于过期描述，已更正。

## v0.6.0 · 2026-09-26

### 新增

- **上传 PDF / Word 自动解析**：`POST /api/materials/extract` 接收文件并抽取纯文本。PDF 逐页解析
  并记录页面边界（`materials.source_map`），所以「答案出处」能定位到具体页码；Word 走 `.docx` 正文
  提取并如实说明没有固定页码。解析结果先回填到表单由用户确认，**不落库、不计额度**，保存才走原接口。
  扫描件、加密 PDF、旧版 `.doc`、超限文件都给中文可操作的提示，而不是抛原始异常。
- **材料问答（答案带出处）**：`POST /api/materials/[id]/ask`。先在材料内做词面检索（TF × IDF，
  英文按词、中文按二元组），再让模型带着 `[n]` 编号回答，最后由代码逐条校验引注：
  越界编号会被删掉并记为问题，没有出处的实质性句子会被单独列出，「模型补充」必须自带标签。
- **检索不到就不生成**：材料里找不到相关句子时直接如实回答「材料里没有直接说明」，
  不调用模型、不消耗额度。没有可用对话模型时降级为「离线摘录」，只摘原文不做生成。
- 新增额度维度 `ask`（默认 200 次/天），额度卡与设置页同步展示。
- 新增依赖 `pdfjs-dist`（PDF）与 `mammoth`（Word）——二进制格式无法用现有依赖解析，这是必要引入。

### 修复

- **英文提问检索不到任何东西**：检索复用了相似度分词器，它为了比较整句会去掉空白，
  结果整段英文被当成一个 token（`vectorsearchencodes...`），英文问题与英文材料永远对不上。
  检索改用独立分词（英文按单词并做保守的复数还原，中文按二元组）。
- **中文 PDF 解析出满篇错字**：部分 PDF 生成器把「一/文/用」写成康熙部首区的兼容字形
  （`⼀`/`⽂`/`⽤`），存进材料后不仅显示异常，中文分词也会整块失效。
  新增只针对 CJK 兼容区的字形归一化——不做整段 NFKC，以免把全角标点也换掉、破坏逐字溯源。
- **材料保存接口没有透传页面映射**：解析出来的 `sourceMap` 在保存时被丢掉，页码定位静默失效；
  现在会经过 schema 校验后再落库（客户端回传的数据一律不可信）。

## v0.5.0 · 2026-09-26

### 新增

- **间隔重复复习（FSRS-5）**：错题不再只是被记下来。交卷后失分的题目立刻进入复习队列
  （当天到期、可立刻重来），复习页按到期顺序出示卡片；答对/答错先用判定链路给分，
  再把分数映射成 FSRS 评分（1–4）推进 stability/difficulty，算出下一次到期时间。
  新增 `/reviews` 页面、`GET /api/reviews`（到期队列 + 统计）、`POST /api/reviews/grade`
  （判定推进 + 自评两种模式），仪表盘与结果页显示待复习数量。
- **自评兜底**：手写作答等无法自动判定的场景可以用 `rating=1..4` 自评推进，不阻塞复习节奏。
- 数据表 `review_items` / `review_logs` + 迁移 `drizzle/0001_loud_lilith.sql`。

### 修复

- **离线出题器会静默少出题**：简答题分支在轮到的知识点不足 3 条原文时直接 `break`，
  把后面所有知识点一并跳过。现在改为「换一个够长的知识点再试」，题量不再缩水。

### 测试

- 新增 `tests/integration/reviews.test.ts`（错题入队 / 答对推进 / 待复核不入队）、
  `tests/unit/fsrs.test.ts`，以及 HTTP 层「复习闭环」用例（交卷 → 到期队列 → 判定推进 → 自评 → 参数校验）。

## v0.4.1 · 2026-09-25

### 修复

- **手机端顶栏被挤断**：`<640px` 时品牌不再换行、导航改为横向可滚动、隐藏邮箱、
  按钮紧凑化——此前在 iPhone 上会出现「材 料 / 错 题 本」竖排断开。
- **落地页文案面向普通用户重写**：原来的首屏在讲「决策模型 / noul / 离线词面判定」，
  对不懂 AI 的备考用户完全是噪声；现在第一屏只讲一件事（把资料变成考卷、告诉你哪里没学会），
  技术解释折叠进「技术细节（给开发者）」。

## v0.3.1 · 2026-09-24

### 新增

- **响应式截图**：每张界面图生成 1440 / 960 / 640 三档，另为内容密集的图生成手机专用裁剪
  （裁到 ~500-640px 宽再放大，390px 屏上文字接近 1:1），页面用 `<picture>` 按视口切换。
- **点击放大**：站点自带零依赖 lightbox（本地 JS/CSS，无 CDN），手机上点击截图进入全屏，
  可横向滚动与双指缩放；桌面端鼠标悬停显示 `zoom-in`。
- **移动端提示**：`<760px` 时截图右上角出现「⤢ 点击放大」角标，避免读者以为看到的只是小图。

### 变更

- README 中的截图改用 960px 版本（桌面显示无损失，体积下降约 40%）。

## v0.3.0 · 2026-09-24

### 新增

- **逐题判定（渐进式）**：作答页把判定拆成 `POST /api/exams/[id]/judge`（逐题）+
  `POST /api/exams/[id]/finalize`（收卷）。界面并发 4 路判定并实时显示每题结果、
  进度条与当前均分，不再让用户对着一个转圈等全部结束。`/submit` 保留为一次性入口。
- **命令行不再是内部脚本**：新增 `bin/jev-exam.mjs` 与 npm 包配置，
  `npx jev-exam verify/grade/render/answer-template` 可直接使用（源码即执行体，无需构建）。
- **MCP server**：`scripts/mcp-server.ts` 暴露 `verify_exam` / `answer_template` /
  `grade_answers` / `render_report` 四个工具，Claude Code、Codex、Cursor 可直接调用。
- **单 key 配置**：新增 `AI_API_KEY`，provider 从 key 形状推断（Anthropic / OpenRouter /
  Google / Groq / xAI / Vercel Gateway / OpenAI），并支持 `AI_PROVIDER`、`AI_BASE_URL`、`CHAT_MODEL`；
  Vercel 一键部署只需填一个变量。只有出题 key 时，判定回落到 LLM 基线而不是离线演示引擎。
- **额度可视化**：额度卡带进度条、重置倒计时与 BYOK 提示（仪表盘与设置页共用）。
- **深色模式**：语义变量整体翻转 + 顶栏手动切换（首屏前应用，无闪白）。
- **报告可定制**：`renderReportHtml` 支持 `brand` / `accent` / `footerNote` / `customCss`。
- **Agent 协作文件**：`AGENTS.md`、`.cursor/rules/jev-exam.mdc`、`llms.txt`。
- **CI**：`.github/workflows/ci.yml` 跑 typecheck → test → build → demo → eval。
- **架构文档**：`docs/architecture.md`（数据流、取舍、验证数字、交付形态）。

### 修复

- MCP server 在 `process.exit` 前未等 stdout 排空，导致十几 KB 的报告响应被截断
  （客户端会报 “Unterminated string”）；现在逐行等待写入完成，并自然退出。

## v0.2.0 · 2026-09-24

### 新增

- **溯源契约**：材料事实（`source_anchor`、`evidence_span`）与模型补充（题干、参考答案、得分点表述、
  判定概率）显式分离，前者必须在原文中逐字定位，否则记为违规（`src/lib/provenance.ts`）。
- **覆盖率校验**：把材料切成要点单位，列出没有被任何题目覆盖的部分，报告与页面如实显示
  「覆盖 9/14」以及未覆盖的句子（`src/lib/coverage.ts`）。
- **不可信材料处理**：扫描并要求出题模型把材料当数据（`src/lib/security/untrusted.ts`），
  检测提示注入、角色劫持、脚本注入、分隔符冲突等模式；拼接提示词前转义分隔符。
- **命令行工具**：`scripts/study.ts` 提供 `verify` / `grade` / `render` / `answer-template` / `demo`
  五个子命令，可脱离 Web 应用直接校验、判定并产出离线静态报告。
- **Agent Skill 入口**：`SKILL.md`，可在 Codex / Claude Code 等支持 Agent Skills 的环境里直接使用。
- **离线演示报告**：`docs/demo/report.html`，由上面的工具链生成，打开即可看到逐点判定与覆盖情况。
- **仓库门面**：MIT LICENSE、SECURITY.md、英文 README、Banner、requirements 与兼容性说明。

### 变更

- `noul` 问题不再共享同一段指令：每个得分点的问题自带它所判的那一点（否则模型无法区分在判哪一点）。
- 离线演示引擎会解析指令里的路径引用（如 `point.statement`），并且不再把学生自己的作答混进比对目标。
- Markdown 标题不再被当作知识要点或覆盖单位。
- 出处定位从「第 N 段」改为「第 N 句 / 共 M 句」，更贴近实际的知识点粒度。

### 验证

- 单元 / 集成测试覆盖判定、门控、覆盖率、溯源契约、注入扫描、报告渲染与 Postgres 数据层（PGlite 实测）。
- 判定评测脚本内置金标准集（12 道主观题 / 42 个得分点），门槛为逐点准确率 ≥ 90% 且校准单调。

## v0.1.0 · 2026-09-23

- 首个版本：材料上传 → 知识点切分 → 出题 → 逐得分点判定 → 错题本与掌握度。
- 判定层抽象为 `DecisionEngine`（TypeSafe Jev / LLM 基线 / 离线演示）。
- 邀请制登录、每日额度、BYOK 加密存储、Postgres + 内存双实现。
