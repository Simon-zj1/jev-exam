# 安全边界

这个项目的输入是**用户自己上传的材料**，输出是**自动生成的判定与报告**。两件事都必须按不可信处理。

## 1. 材料是不可信数据

- 材料会被拼进提示词交给出题模型。材料里出现的任何指令、角色设定、越权要求或格式声明
  都**不会被执行**：系统提示词里明确声明了这条边界，代码侧还会扫描并提示可疑内容。
- `src/lib/security/untrusted.ts` 会检测以下几类模式并在创建材料时提示用户：
  要求模型忽略既有指令、角色劫持、探测系统提示词、要求执行命令、HTML/脚本注入、
  诱导外发数据、与提示分隔符冲突的符号、双向文本控制字符。
- 扫描结果只用于提示与记录，**不会**修改材料内容。真正的兜底是下一条。

## 2. 材料事实必须可定位

判定是否可信，取决于「材料事实」能不能回到原文。项目把它写成显式契约
（`src/lib/provenance.ts`）：

| 字段 | 来源 | 约束 |
| --- | --- | --- |
| `question.source_anchor` | 材料原文 | 必须逐字出现在材料中 |
| `rubric_point.evidence_span` | 材料原文 | 必须逐字出现在材料中 |
| 题干、选项、参考答案、得分点表述、判定概率 | 模型补充 | 允许改写，不作为材料引用 |

任何「材料原文」字段定位失败都会被记为违规并在报告里如实列出。这样即使注入成功，
伪造的内容也无法被当成材料事实展示。

## 3. 覆盖率不假装完整

`src/lib/coverage.ts` 会把材料切成要点单位，列出**没有被任何题目覆盖**的部分。
报告与页面会如实显示「覆盖 9/14」以及未覆盖的具体句子，而不是暗示这份试卷覆盖了全部内容。

## 4. 渲染安全

- 材料与模型输出在写进静态报告前全部经过 HTML 转义（`escapeHtml`）。
- 生成的报告是**单文件、离线**的：不引用任何外部字体、脚本或图片，也不发起网络请求。
- Web 界面通过 React 渲染文本，不使用 `dangerouslySetInnerHTML`。

## 5. 密钥与数据

- 用户的 BYOK 密钥使用 AES-256-GCM 加密后存库（密钥由 `SESSION_SECRET` 经 scrypt 派生），
  仅在该用户自己的请求中使用，接口不会回显，也不会下发到浏览器。
- 会话使用 HttpOnly 签名 Cookie。
- 材料默认私有，删除材料会一并删除其派生的大纲、试卷、作答与判定记录。
- 上传内容不用于训练模型。

## 6. 已知限制

- 演示用的离线判定引擎（`lexical-demo`）只是词面近似，**不能用于真实评分**；
  它存在的原因是让整条链路在没有 API key 时也能被测试。评测脚本在金标准集上会给出它的真实分数。
- 判定结果是概率，不是事实。低置信度的题目会被标记为「待复核」并给出分数区间，
  但复核本身目前需要人工完成。
- 暂不支持 PDF/扫描件解析；OCR 相关内容进入材料前请自行确认风险。

## 报告问题

请通过 GitHub Issues 提交，或在 [www.simon-zj.top](https://www.simon-zj.top) 的联系方式找到我。
